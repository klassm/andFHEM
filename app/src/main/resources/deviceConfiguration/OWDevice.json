{
  "sensorDevice": false,
  "defaultGroup": "UNKNOWN",
  "sanitise": {
    "deviceValues": {
      "STATE": {
        "replaceAll": [
          {
            "search": "([0-9\\.]+) ([^0-9]+)",
            "replaceBy": "$1 ($2)"
          },{
            "search": "([0-9\\.]+)(°C)",
            "replaceBy": "$1 ($2)"
          }
        ]
      }
    }
  }
}